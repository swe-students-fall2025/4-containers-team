# MongoDB Configuration
# 
# This file is optional. If you want to use a custom MongoDB connection string,
# create a .env file in the root directory and set MONGO_URI.
#
# If MONGO_URI is not set, the application will automatically construct the
# connection string from MONGODB_HOST and MONGODB_PORT (which are set in docker-compose.yml).

# Optional: Full MongoDB connection URI
# Example for local MongoDB:
# MONGO_URI=mongodb://mongodb:27017/
#
# Example for MongoDB with authentication:
# MONGO_URI=mongodb://username:password@mongodb:27017/
#
# Example for external MongoDB Atlas:
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
#
# Leave empty to use default connection (built from MONGODB_HOST and MONGODB_PORT)
MONGO_URI=
